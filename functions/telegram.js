async function sendTelegramMessage() {
  // TODO: integrate Telegram notifications when credentials are ready.
  return { ok: false, reason: "not_implemented" };
}

module.exports = {
  sendTelegramMessage
};
